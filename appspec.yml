version: 0.0
os: linux
files:
  - source: /
    destination: /home/build
    overwrite: yes

permissions:
  - object: /home
    pattern: "**"
    owner: root
    group: root

hooks:
  AfterInstall:
    - location: scripts/after-deploy.sh
      timeout: 300
      runas: root

name: deploy lolink-back

on:
  pull_request:
    branches:
      - staging
    paths:
      - lolink_back*/**
    types:
      - closed

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source code.
        uses: actions/checkout@master
